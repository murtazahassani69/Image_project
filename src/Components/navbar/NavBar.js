import React from 'react'
import AppBar  from 'material-ui/AppBar'
const NavBar  = () => <AppBar title="image finder" />;

// in navbar folder will build the title of application

export default NavBar